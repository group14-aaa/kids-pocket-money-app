///get current user. 
    ///If user = kid, then get local storage history for the kid, display task history to history html page
    //if user = parent, then get parent kid association, then get local storage for any kids and display on history html page
    //


    $(document).ready(function () {

        //get kid-email 
 
    // Get parent-kid association //ammend
    // if (currentUser.userType === 'parent' && currentUser.parentEmail === '') {
    //     const parentEmail = currentUser.email;
    //     const associatedKids = existingUsers.filter(kid => kid.parentEmail === parentEmail);
        
    //     if (associatedKids.length === 0) {
    //       const noKidsAvailable = `<h4 class="text-center">No kids available<h3/>`
    //       associationList.append(noKidsAvailable);
    //     }
  
    //     if (associatedKids.length > 0) {
    //       // Iterate through associatedKids and retrieve tasks from local storage
    //       associatedKids.forEach(kid => {

    //         //


    //       });
    //     }
    //   }



    // function getTaskHistory(){

    // const kidStoredTasks = JSON.parse(currentUser.localStorage.getItem('kidTasks'));

  


// getTaskHistory();

  // Load task history to the page
//   function loadTaskHistoryToPage() {
//       const taskHistory = getLocalStorageItem("taskHistory", []);
//       const taskHistoryContainer = $("#task-history-container");
//       taskHistoryContainer.empty();

//       const currentUser = getCurrentUser();

//       taskHistory.forEach(transaction => {
//           if (currentUser.email === task.parentEmail || currentUser.email === task.kidEmail) {
//               displayTask(task);
//           }
//       });
//   }

  // Display a task card
//   function displayTask(Task) {
//       const cardTemplate = `
//           <div class="col-md-4">
//               <div class="card mb-4">
//                   <div class="card-body">
//                       <h5 class="card-title">Task Details</h5>
//                       <p>Kid: ${Task.kidEmail}</p>
//                       <p>Amount: £${Task.amount.toFixed(2)}</p>
//                       <p>Status: ${Task.status}</p>
//                   </div>
//               </div>
//           </div>`;
//       $("# Task-history-container").append(cardTemplate);
//   }


});
